import { Button } from "@mui/material";

const HomePage = () => {
  return (
    <div>
      <h1>HomePage</h1>
      <Button variant="contained">Contained</Button>
    </div>
  );
};

export default HomePage;
